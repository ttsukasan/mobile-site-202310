{
  "name": "mobile-site-202310",
  "version": "0.0.1",
  "description": "description",
  "keywords": [""],
  "license": "",
  "author": "ttsukasan",
  "scripts": {
    "build": "parcel build index.html -d ./docs --public-url /animal-combine-puzzle/",
    "dev": "parcel index.html --open",
    "start": "npm run build && npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4"
  }
}
